<header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="#"></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor02">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('index_library') }}">Home <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('book_list') }}">Les livres</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('authors_list') }}">Les auteurs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                    </li>
                </ul>
                <form class="form-inline my-2 my-lg-0" action="{{ path('book_search') }}" method="get">

                    {% if authors is defined %}
                        <select class="custom-select" id="inputGroupSelect01" name="author">
                            <option selected value="">chercher par auteur</option>
                            {% for author in authors %}
                                <option value="{{ author.id }}">{{ author.lastName }} {{ author.firstName }}</option>
                            {% endfor %}
                        </select>
                    {% endif %}

                    <input class="form-control mr-sm-2" type="text" placeholder="Search" name="titre">

                    <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
                </form>
            </div>
        </nav>

        <div class="backgroundLivre">
            <img src="{{ asset('assets/img/backgroundLivre.jpg') }}" alt="">
        </div>


</header>